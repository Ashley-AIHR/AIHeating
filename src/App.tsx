import { useEffect, useMemo, useState, type ReactNode } from 'react';
import {
  advancePrototypeTimeline, applyPrototypeControl, applyPrototypeRecommendation, buildingProfiles, buildings, equipmentLimits,
  getFixtureOptimisedState, modelMetrics, network as fixtureNetwork, overviewFixture, recommendationFixture, resultComparison,
  scenario, selectAvoidableOversupply, selectBuildingForecasts, selectComfortRate, selectComplianceRate, selectDistributionBins,
  selectForecastForHorizon, selectForecastInsights, selectForecastKpis, selectOverheatingRate, selectResultsComparison,
  selectTemperaturePercentiles, selectUnderheatingRate, timeline, type Building, type ControlMode, type ForecastHorizon,
  type HeatingNetworkState, type Language, type SemanticActionRecord, type ZoneKey,
} from './domain';
import { translate } from './i18n';
import { finiteChartSegments } from './chart';

type T = (key: string) => string;
const modeKeys: ControlMode[] = ['traditional', 'advisory', 'optimised'];
const zoneKeys: ZoneKey[] = ['near', 'mid', 'far'];
const horizonKeys: ForecastHorizon[] = [6, 24, 48];
const eventLabels: Record<string, string> = { simulation_started: 'eventSimulationStarted', simulation_reset: 'eventSimulationReset', control_mode_changed: 'eventControlModeChanged', mpc_recommendation_generated: 'eventMpcRecommendationGenerated', mpc_control_auto_applied: 'eventMpcControlAutoApplied', recommendation_applied: 'eventRecommendationApplied', simulation_step: 'eventSimulationStep' };
if (typeof document !== 'undefined' && !document.getElementById('p0-sr-style')) { const style = document.createElement('style'); style.id = 'p0-sr-style'; style.textContent = '.sr-only{position:absolute!important;width:1px!important;height:1px!important;padding:0!important;margin:-1px!important;overflow:hidden!important;clip:rect(0,0,0,0)!important;white-space:nowrap!important;border:0!important}.delay-summary{display:flex;align-items:center;gap:8px;margin-left:auto;color:#536b8f;font-size:10px}.delay-summary strong{color:#173b94}.delay-summary span{white-space:nowrap}.thermal-state-legend{display:flex;align-items:center;gap:7px;flex-wrap:wrap}.thermal-state-legend strong{color:#173b94;margin-right:2px}.thermal-chip{display:inline-flex;align-items:center;gap:3px;white-space:nowrap}.thermal-chip i{width:8px;height:8px;border-radius:50%;display:inline-block}'; document.head.appendChild(style); }

function useT(language: Language): T { return (key) => translate(language, key); }
function interpolate(value: string, replacements: Record<string, string | number>): string { return Object.entries(replacements).reduce((text, [key, replacement]) => text.split(`{${key}}`).join(String(replacement)), value); }
function pct(value: number): string { return `${Math.round(value * 100)}%`; }
function cx(...values: Array<string | false | undefined>): string { return values.filter(Boolean).join(' '); }
function Icon({ symbol, tone = '' }: { symbol: string; tone?: string }) { return <span className={cx('icon', tone)} aria-hidden="true">{symbol}</span>; }

function KpiCard({ icon, label, value, note, tone = 'blue' }: { icon: string; label: string; value: string; note?: string; tone?: string }) {
  return <div className="kpi-card"><Icon symbol={icon} tone={tone} /><div className="kpi-copy"><div className="eyebrow">{label}</div><div className="kpi-value">{value}</div>{note && <div className={cx('kpi-note', tone === 'red' ? 'negative' : 'positive')}>{note}</div>}</div></div>;
}

function Panel({ title, subtitle, children, className = '', action }: { title?: string; subtitle?: string; children: ReactNode; className?: string; action?: ReactNode }) {
  return <section className={cx('panel', className)}>{(title || action) && <div className="panel-heading"><div>{title && <h2>{title}</h2>}{subtitle && <p>{subtitle}</p>}</div>{action}</div>}{children}</section>;
}

function LineChart({ values, values2, labels, color = '#1378f6', color2 = '#ee3d45', min, max, unit, ariaLabel }: { values: Array<number | null | undefined>; values2?: Array<number | null | undefined>; labels: string[]; color?: string; color2?: string; min?: number; max?: number; unit?: string; ariaLabel?: string }) {
  const all = [...values, ...(values2 ?? [])].filter((value): value is number => Number.isFinite(value));
  const lo = min ?? Math.min(...all, 0) - 1; const hi = max ?? Math.max(...all, 0) + 1;
  const x = (index: number) => (index / Math.max(1, labels.length - 1)) * 600;
  const y = (value: number) => 96 - ((value - lo) / Math.max(1, hi - lo)) * 78;
  const pathFor = (series: Array<number | null | undefined>) => finiteChartSegments(series).map((segment) => <path key={segment.map((point) => point.index).join('-')} d={segment.map((point, i) => `${i ? 'L' : 'M'} ${x(point.index)} ${y(point.value)}`).join(' ')} fill="none" stroke={series === values ? color : color2} strokeWidth="3" strokeLinecap="round" strokeLinejoin="round" />);
  return <div className="chart-wrap"><svg viewBox="0 0 600 150" role="img" aria-label={ariaLabel ?? `${unit ?? 'Value'} chart`} preserveAspectRatio="none">{[18, 44, 70, 96].map((gridY) => <line key={gridY} x1="0" x2="600" y1={gridY} y2={gridY} className="grid-line" />)}{pathFor(values)}{values2 && pathFor(values2)}{values.map((value, i) => Number.isFinite(value) ? <circle key={`${value}-${i}`} cx={x(i)} cy={y(value as number)} r="3.5" fill={color}><title>{`${labels[i] ?? ''}: ${(value as number).toFixed(1)}${unit ? ` ${unit}` : ''}`}</title></circle> : null)}</svg><div className="chart-labels">{labels.map((label, i) => <span key={`${label}-${i}`}>{label}</span>)}</div></div>;
}

function BarChart({ labels, values, values2, color = '#9aa9ba', color2 = '#1378f6', unit, ariaLabel }: { labels: string[]; values: number[]; values2?: number[]; color?: string; color2?: string; unit?: string; ariaLabel?: string }) {
  const max = Math.max(...values, ...(values2 ?? []), 1);
  return <div className="bar-chart" role="img" aria-label={ariaLabel ?? unit}><div className="bar-grid"><span>{max.toFixed(0)}</span><span>{(max / 2).toFixed(0)}</span><span>0</span></div><div className="bar-groups">{labels.map((label, i) => <div className="bar-group" key={`${label}-${i}`}><div className="bars"><div className="bar" style={{ height: `${(values[i] / max) * 100}%`, background: color }}><b>{values[i].toFixed(values[i] < 10 ? 1 : 0)}</b></div>{values2 && <div className="bar" style={{ height: `${(values2[i] / max) * 100}%`, background: color2 }}><b>{values2[i].toFixed(values2[i] < 10 ? 1 : 0)}</b></div>}</div><span>{label}</span></div>)}</div>{unit && <div className="chart-unit">{unit}</div>}</div>;
}

function Legend({ items }: { items: Array<{ label: string; color: string; dashed?: boolean }> }) { return <div className="legend">{items.map((item, i) => <span key={`${item.label}-${i}`}><i style={{ background: item.color, borderTop: item.dashed ? `2px dashed ${item.color}` : undefined }} />{item.label}</span>)}</div>; }
function ModeSelector({ mode, setMode, t }: { mode: ControlMode; setMode: (mode: ControlMode) => void; t: T }) { return <div className="mode-selector">{modeKeys.map((key) => <button key={key} className={cx('mode-btn', mode === key && 'active')} onClick={() => setMode(key)}>{t(key)}</button>)}</div>; }

function NetworkMap({ t, networkState, buildingItems, onBuildingClick }: { t: T; networkState: HeatingNetworkState; buildingItems: Building[]; onBuildingClick?: (building: Building) => void }) {
  const positions = [{ x: 425, y: 74 }, { x: 505, y: 74 }, { x: 425, y: 174 }, { x: 505, y: 174 }, { x: 675, y: 74 }, { x: 755, y: 74 }, { x: 675, y: 174 }, { x: 755, y: 174 }, { x: 925, y: 74 }, { x: 1005, y: 74 }, { x: 925, y: 174 }, { x: 1005, y: 174 }];
  const zoneMeta = { near: { x: 380, width: 180, color: '#ed6a72' }, mid: { x: 600, width: 180, color: '#20b887' }, far: { x: 820, width: 220, color: '#2e83f5' } };
  return <div className="network-map"><svg viewBox="0 0 1150 300" role="img" aria-label={t('secondaryNetwork')}>{zoneKeys.map((zone) => <g key={zone}><rect x={zoneMeta[zone].x} y="26" width={zoneMeta[zone].width} height="238" rx="10" fill={`${zoneMeta[zone].color}0c`} stroke={zoneMeta[zone].color} strokeDasharray="6 5" /><text x={zoneMeta[zone].x + zoneMeta[zone].width / 2} y="51" textAnchor="middle" className="zone-label" fill={zoneMeta[zone].color}>{t(zone)}</text></g>)}<g className="station"><rect x="90" y="113" width="112" height="75" rx="8" /><path d="M103 133h86v42h-86zM125 143h14v24h-14zm25 0h14v24h-14z" /><text x="146" y="213" textAnchor="middle">{t('substation')}</text><text x="146" y="250" textAnchor="middle" className="station-value">{networkState.currentHeatSupplyMw.toFixed(2)} MW</text></g><path d="M204 138H1070" stroke="#ef3946" strokeWidth="8" strokeLinecap="round" /><path d="M204 174H1070" stroke="#1479f5" strokeWidth="8" strokeLinecap="round" /><text x="26" y="85" className="network-note">{t('primaryNetwork')}</text>{buildingItems.map((building, i) => { const p = positions[i]; const zone = networkState.zones[building.zone]; const temp = building.indoorTemperatureC; const tone = temp < 18 ? '#4b93ed' : temp < 20 ? '#63a9ef' : temp <= 22 ? '#22b786' : temp <= 23 ? '#ee9b22' : '#ec424b'; return <g key={building.id} className="building-node" onClick={() => onBuildingClick?.(building)} tabIndex={0} role="button" aria-label={`${building.id}, ${temp.toFixed(1)}°C`}><line x1={p.x + 26} x2={p.x + 26} y1={p.y + 36} y2="138" stroke="#ef3946" strokeWidth="5" /><line x1={p.x + 38} x2={p.x + 38} y1={p.y + 36} y2="174" stroke="#1479f5" strokeWidth="5" /><path d={`M${p.x} ${p.y + 18}l26-19 26 19v28H${p.x}z`} fill={tone} opacity=".92" /><path d={`M${p.x - 5} ${p.y + 18}L${p.x + 26} ${p.y - 5}l31 23`} fill="none" stroke={tone} strokeWidth="8" /><rect x={p.x - 12} y={p.y + 48} width="76" height="31" rx="7" fill="white" stroke="#d6e2f0" /><text x={p.x + 26} y={p.y + 62} textAnchor="middle" className="building-id">{building.id}</text><text x={p.x + 26} y={p.y + 75} textAnchor="middle" className="building-temp" fill={tone}>{temp.toFixed(1)}°C</text><title>{`${building.id} · ${temp.toFixed(1)}°C · ${zone.flowM3h.toFixed(1)} m³/h`}</title></g>; })}</svg><div className="network-legend"><span><i className="line-hot" />{t('supplyHot')}</span><span><i className="line-cold" />{t('returnCold')}</span><span className="thermal-state-legend" aria-label={t('thermalState')}><strong>{t('thermalState')}</strong>{[['thermalCold', '#4b93ed', '<18°C'], ['thermalCool', '#63a9ef', '18–20°C'], ['thermalComfort', '#22b786', '20–22°C'], ['thermalWarm', '#ee9b22', '22–23°C'], ['thermalOverheated', '#ec424b', '>23°C']].map(([key, color, range]) => <span className="thermal-chip" key={key}><i style={{ background: color }} />{t(key)} {range}</span>)}</span><span className="delay-summary" aria-label={t('estimatedDelay')}><strong>{t('estimatedDelay')}</strong><span>{t('near')} 10 min</span><span>{t('mid')} 20 min</span><span>{t('far')} 35 min</span></span></div></div>;
}

function Recommendation({ t, mode, recommendation, networkState, onApply, applied }: { t: T; mode: ControlMode; recommendation: typeof recommendationFixture; networkState: HeatingNetworkState; onApply: () => void; applied: boolean }) {
  return <div className={cx('recommendation', mode === 'optimised' && 'optimised')}><div className="recommendation-head"><span className="bulb">✦</span><div><h3>{t('recommendation')}</h3><p>{t('fixture')}</p></div><span className="status-pill">{mode === 'optimised' ? `● ${t('mpcActive')}` : mode === 'advisory' ? 'AI' : t('traditionalShort')}</span></div><p className="recommendation-text">{t('recommendationText')}</p><div className="recommendation-values"><span>{recommendation.supplyTemperatureC}°C <small>{t('supply')}</small></span><span>{recommendation.pumpFrequencyHz} Hz <small>{t('pumpFrequency')} · {networkState.differentialPressureKpa} kPa</small></span><span>{t('near')} {recommendation.valves.near}% · {t('mid')} {recommendation.valves.mid}% · {t('far')} {recommendation.valves.far}%</span></div>{mode === 'advisory' && <button className="primary-btn full" onClick={onApply}>{applied ? `✓ ${t('applyDone')}` : t('apply')}</button>}{mode === 'optimised' && <div className="auto-note">✓ {t('autoApplied')}</div>}{mode === 'traditional' && <div className="muted-note">{t('noAutoChanges')}</div>}</div>;
}

function SystemStatus({ t, networkState }: { t: T; networkState: HeatingNetworkState }) { return <Panel title={t('systemStatus')} action={<span className="operating"><i />{t('operating')}</span>}><div className="status-rows">{[[t('supply'), `${networkState.supplyTemperatureC.toFixed(1)} °C`], [t('return'), `${networkState.returnTemperatureC.toFixed(1)} °C`], [t('totalFlow'), `${networkState.totalFlowM3h.toFixed(1)} m³/h`], [t('pressure'), `${networkState.differentialPressureKpa} kPa`], [t('pumpFrequency'), `${networkState.pumpFrequencyHz} Hz`]].map(([label, value]) => <div key={label}><span>{label}</span><strong>{value}</strong></div>)}</div><div className="zone-status">{zoneKeys.map((zone) => <div key={zone}><span>{t(zone)}</span><b>{networkState.zones[zone].flowM3h.toFixed(1)} <small>m³/h</small></b><em>{networkState.zones[zone].valveOpeningPct}%</em></div>)}</div></Panel>; }

function Overview({ t, mode, recommendation, networkState, buildingItems, setSelectedBuilding, onApply, applied }: { t: T; mode: ControlMode; recommendation: typeof recommendationFixture; networkState: HeatingNetworkState; buildingItems: Building[]; setSelectedBuilding: (b: Building) => void; onApply: () => void; applied: boolean }) {
  const percentiles = selectTemperaturePercentiles(buildingItems);
  return <div className="page"><div className="page-intro"><div><p className="kicker">{t('staticPrototype')}</p><h1>{t('overview')}</h1><p>{t('resultsDescription')}</p></div><span className="live-badge"><i />{t('staticPrototype')}</span></div><div className="kpi-grid overview-kpis"><KpiCard icon="♨" label={t('outdoor')} value={`${scenario.weather.outdoorTemperatureC.toFixed(1)} °C`} note="+0.3°C" tone="blue" /><KpiCard icon="♨" label={t('requiredHeat')} value={`${networkState.requiredHeatLoadMw.toFixed(2)} MW`} note="↑ 1.8%" tone="orange" /><KpiCard icon="♨" label={t('currentSupply')} value={`${networkState.currentHeatSupplyMw.toFixed(2)} MW`} note="↑ 13%" tone="orange" /><KpiCard icon="✓" label={t('compliance')} value={pct(selectComplianceRate(buildingItems))} note="↑ 5.6%" tone="green" /><KpiCard icon="⌂" label={t('comfort')} value={pct(selectComfortRate(buildingItems))} note={`${t('distribution')}: ${percentiles.p50.toFixed(1)}°C`} tone="green" /><KpiCard icon="!" label={t('overheating')} value={pct(selectOverheatingRate(buildingItems))} note="↑ 4.2%" tone="red" /><KpiCard icon="⚡" label={t('pumpPower')} value={`${networkState.pumpPowerKw} kW`} note="↓ 12%" tone="blue" /></div><div className="content-grid overview-main"><Panel className="network-panel" title={t('secondaryNetwork')} subtitle={t('fixture')}><NetworkMap t={t} networkState={networkState} buildingItems={buildingItems} onBuildingClick={setSelectedBuilding} /></Panel><div className="overview-rail"><SystemStatus t={t} networkState={networkState} /><Panel title={t('nextForecast')}><div className="forecast-risk"><span>⚠ {t('riskOver')}</span><b className="text-red">{overviewFixture.next2hOverheatingCount} / 12</b><span>❄ {t('riskUnder')}</span><b>{overviewFixture.next2hUnderheatingCount} / 12</b></div></Panel><Panel title={t('alerts')}><div className="alert-card danger"><b>⚠ {t('windowRisk')}</b><p>{t('overheatingInsightText')}</p></div><div className="alert-card"><b>ⓘ {t('farRisk')}</b><p>{t('farRiskText')}</p></div><div className="alert-card success"><b>✓ {t(overviewFixture.complianceAlertKey)}</b></div></Panel><Recommendation t={t} mode={mode} recommendation={recommendation} networkState={networkState} onApply={onApply} applied={applied} /></div></div></div>;
}

function Simulation({ t, mode, recommendation, setMode, networkState, setNetworkState, buildingItems, selectedBuilding, setSelectedBuilding, onApply, applied, frameIndex, simulationTime, isPlaying, setIsPlaying: setPlaying, onStep, onReset = () => window.location.reload(), events, clearEvents }: { t: T; mode: ControlMode; recommendation: typeof recommendationFixture; setMode: (mode: ControlMode) => void; networkState: HeatingNetworkState; setNetworkState: (state: HeatingNetworkState) => void; buildingItems: Building[]; selectedBuilding: Building | null; setSelectedBuilding: (b: Building | null) => void; onApply: () => void; applied: boolean; frameIndex: number; simulationTime: string; isPlaying: boolean; setIsPlaying: (value: boolean) => void; onStep: () => void; onReset?: () => void; events: SemanticActionRecord[]; clearEvents: () => void }) {
  const selected = selectedBuilding ?? buildingItems[2]; const frame = timeline[frameIndex]; const setIsPlaying = (value: boolean) => { if (!value && !isPlaying) onReset(); else setPlaying(value); };
  const setValue = (key: 'supplyTemperatureC' | 'pumpFrequencyHz', value: number) => setNetworkState(applyPrototypeControl(networkState, { [key]: value }));
  const eventDetail = (event: SemanticActionRecord) => interpolate(t(event.detailKey), { time: event.detailValue ?? '', mode: event.detailValue ?? '' });
  return <div className="page"><div className="page-intro"><div><p className="kicker">{t('playback')}</p><h1>{t('simulation')}</h1><p>{t('simulationDescription')}</p></div><div className="playback"><button className="primary-btn" onClick={() => setIsPlaying(!isPlaying)}>{isPlaying ? `Ⅱ ${t('pause')}` : `▶ ${t('play')}`}</button><button className="ghost-btn" onClick={onStep}>▷ {t('step')}</button><button className="ghost-btn" onClick={onReset}>↻ {t('reset')}</button></div></div><div className="sim-top-cards"><KpiCard icon="♨" label={t('outdoor')} value={`${frame.outdoorTemperatureC.toFixed(1)} °C`} note={simulationTime} /><KpiCard icon="☼" label={t('solarRadiation')} value={`${scenario.weather.solarRadiationWm2} W/m²`} note="↑ 28%" tone="orange" /><KpiCard icon="≋" label={t('windSpeed')} value={`${scenario.weather.windSpeedMs} m/s`} note="↓ 12%" tone="blue" /><KpiCard icon="⌘" label={t('controlMode')} value={t(mode)} note={mode === 'optimised' ? t('autoApplied') : t('fixtureData')} tone="green" /></div><div className="content-grid simulation-main"><Panel className="network-panel" title={t('secondaryNetwork')} subtitle={t('fixture')}><NetworkMap t={t} networkState={networkState} buildingItems={buildingItems} onBuildingClick={setSelectedBuilding} /></Panel><div className="sim-rail"><Panel title={t('controlMode')}><ModeSelector mode={mode} setMode={setMode} t={t} /><div className="control-fields"><label>{t('supply')}<input type="number" min={equipmentLimits.supplyTemperatureC.min} max={equipmentLimits.supplyTemperatureC.max} value={networkState.supplyTemperatureC} onChange={(e) => setValue('supplyTemperatureC', Number(e.target.value))} /><small>40–60°C · ±2°C / {t('step')}</small></label><label>{t('pumpFrequency')}<input type="number" min={equipmentLimits.pumpFrequencyHz.min} max={equipmentLimits.pumpFrequencyHz.max} value={networkState.pumpFrequencyHz} onChange={(e) => setValue('pumpFrequencyHz', Number(e.target.value))} /><small>30–50 Hz · ±2 Hz / {t('step')}</small></label>{zoneKeys.map((zone) => <label key={zone}>{t(zone)} {t('valve')}<input type="number" min={equipmentLimits.valveOpeningPct.min} max={equipmentLimits.valveOpeningPct.max} value={networkState.zones[zone].valveOpeningPct} onChange={(e) => setNetworkState(applyPrototypeControl(networkState, { valves: { [zone]: Number(e.target.value) } }))} /><small>20–100% · ±10 pp / {t('step')}</small></label>)}</div></Panel><Recommendation t={t} mode={mode} recommendation={recommendation} networkState={networkState} onApply={onApply} applied={applied} /></div></div><div className="content-grid chart-grid sim-charts"><Panel title={t('indoorByZone')}><Legend items={zoneKeys.map((zone, i) => ({ label: t(zone), color: ['#ef3946', '#22b786', '#277fec'][i] }))} /><LineChart labels={timeline.map((item) => item.time)} values={timeline.map(() => buildingItems.filter((b) => b.zone === 'near').reduce((sum, b) => sum + b.indoorTemperatureC, 0) / 4)} values2={timeline.map(() => buildingItems.filter((b) => b.zone === 'mid').reduce((sum, b) => sum + b.indoorTemperatureC, 0) / 4)} color="#ef3946" color2="#22b786" min={16} max={26} unit="°C" ariaLabel={t('indoorByZone')} /></Panel><Panel title={t('heatLoadVsSupply')}><Legend items={[{ label: t('requiredHeat'), color: '#ef3946' }, { label: t('currentSupply'), color: '#1479f5' }]} /><LineChart labels={timeline.map((item) => item.time)} values={timeline.map(() => networkState.requiredHeatLoadMw)} values2={timeline.map(() => networkState.currentHeatSupplyMw)} color="#ef3946" color2="#1479f5" min={0} max={3.2} unit="MW" ariaLabel={t('heatLoadVsSupply')} /></Panel><Panel title={t('pumpTrend')}><LineChart labels={timeline.map((item) => item.time)} values={timeline.map(() => networkState.pumpPowerKw)} values2={timeline.map((_, i) => i * networkState.pumpPowerKw * 0.45)} color="#1479f5" color2="#8ba5c8" min={0} max={160} unit="kW / kWh" ariaLabel={t('pumpTrend')} /></Panel><Panel title={t('valveTrend')}><Legend items={zoneKeys.map((zone, i) => ({ label: t(zone), color: ['#ef3946', '#22b786', '#277fec'][i] }))} /><LineChart labels={timeline.map((item) => item.time)} values={timeline.map((item) => item.nearValvePct)} values2={timeline.map((item) => item.farValvePct)} color="#ef3946" color2="#277fec" min={20} max={100} unit="%" ariaLabel={t('valveTrend')} /></Panel></div><Panel title={`${t('selectedBuilding')} — ${selected.id}`} className="detail-panel"><div className="detail-grid"><div><span>{t('area')}</span><b>{selected.areaM2} m²</b></div><div><span>{t('year')}</span><b>{selected.year}</b></div><div><span>{t('insulation')}</span><b>{t(selected.insulation.toLowerCase())}</b></div><div><span>{t('zone')}</span><b>{t(selected.zone)}</b></div><div><span>{t('currentIndoor')}</span><b className={selected.indoorTemperatureC > 23 ? 'text-red' : ''}>{selected.indoorTemperatureC.toFixed(1)} °C</b></div><div><span>{t('predictedIndoor')}</span><b>{recommendationFixture.selectedBuildingPredictedC.toFixed(1)} °C</b></div><div><span>{t('zoneFlow')}</span><b>{networkState.zones[selected.zone].flowM3h.toFixed(1)} m³/h</b></div><div><span>{t('transportDelay')}</span><b>{networkState.zones[selected.zone].transportDelayMin} min</b></div></div><button className="close-detail" aria-label={t('close')} onClick={() => setSelectedBuilding(null)}>×</button></Panel><Panel title={t('events')} action={<button className="text-btn" onClick={clearEvents}>{t('clear')}</button>} className="event-panel"><div className="event-log">{events.map((event, i) => <div key={`${event.type}-${i}`}><i className={event.type.includes('mpc') ? 'blue-dot' : 'green-dot'} /><span>{event.at}</span><b>{t(eventLabels[event.type] ?? event.type)}</b><em>{eventDetail(event)}</em></div>)}</div></Panel></div>;
}

function Insight({ number, title, text }: { number: string; title: string; text: string }) { return <div className="insight"><span>{number}</span><div><b>{title}</b><p>{text}</p></div></div>; }

function Forecast({ t, horizon, forecastAsOf, setHorizon }: { t: T; horizon: ForecastHorizon; forecastAsOf: string; setHorizon: (horizon: ForecastHorizon) => void }) {
  const data = selectForecastForHorizon(horizon, forecastAsOf); const kpis = selectForecastKpis(horizon, forecastAsOf); const rows = selectBuildingForecasts(horizon, forecastAsOf); const bins = selectDistributionBins(horizon, forecastAsOf); const insights = selectForecastInsights(horizon, forecastAsOf); const outdoor = data.outdoorTemperatureC; const startOutdoor = outdoor[0]; const endOutdoor = outdoor[outdoor.length - 1];
  const insightTitles = ['rapidWarmingInsightTitle', 'solarInsightTitle', 'overheatingInsightTitle', 'farInsightTitle']; const insightTexts = ['rapidWarmingInsightText', 'solarInsightText', 'overheatingInsightText', 'farInsightText'];
  return <div className="page"><div className="page-intro"><div><p className="kicker">{t('staticPrototype')}</p><h1>{t('forecastTitle')}</h1><p>{t('forecastDescription')}</p><span className="forecast-asof">{interpolate(t('asOf'), { time: data.forecastAsOf })}</span></div><div className="horizon-tabs">{horizonKeys.map((key) => <button className={horizon === key ? 'active' : ''} key={key} onClick={() => setHorizon(key)}>{t(`next${key}`)}</button>)}</div></div><div className="kpi-grid forecast-kpis"><KpiCard icon="♨" label={t('outdoor')} value={`${startOutdoor.toFixed(1)} → ${endOutdoor.toFixed(1)}°C`} note={`${horizon} ${t('hours')}`} /><KpiCard icon="♨" label={t('requiredHeat')} value={`${kpis.requiredHeatLoadMw.toFixed(2)} MW`} note={t('fixtureData')} tone="orange" /><KpiCard icon="♨" label={t('plannedSupply')} value={`${kpis.plannedHeatSupplyMw.toFixed(2)} MW`} note={t('optimisedShort')} tone="orange" /><KpiCard icon="↓" label={t('oversupply')} value={`${kpis.avoidableOversupplyMw.toFixed(2)} MW`} note={`${t('plannedSupply')} − ${t('requiredHeat')}`} tone="green" /><KpiCard icon="⌂" label={t('predictedIndoor')} value={`${data.predictedIndoorTemperatureC.toFixed(1)} °C`} note={`${t('interval')}: ${data.predictionIntervalC[0]}–${data.predictionIntervalC[1]}°C`} tone="green" /><KpiCard icon="!" label={t('overheating')} value={`${data.overheatingRiskPct}%`} note={`${t('confidence')}: ${t(data.confidence.toLowerCase())}`} tone="red" /></div><div className="content-grid forecast-grid"><div className="forecast-charts"><Panel title={interpolate(t('outdoorForecastTitle'), { hours: horizon })}><Legend items={[{ label: t('historicalPast'), color: '#1479f5' }, { label: interpolate(t('forecastNext'), { hours: horizon }), color: '#1479f5', dashed: true }]} /><LineChart labels={data.series.labels} values={data.series.requiredHeatLoadMw.map((value) => value - 6)} values2={data.outdoorTemperatureC} color="#1479f5" color2="#1479f5" min={-8} max={8} unit="°C" ariaLabel={t('outdoorForecastTitle')} /></Panel><Panel title={t('plannedVsRequired')}><Legend items={[{ label: t('requiredHeat'), color: '#ee3d45' }, { label: t('plannedSupply'), color: '#1479f5' }, { label: t('traditionalShort'), color: '#8195af', dashed: true }]} /><LineChart labels={data.series.labels} values={data.series.requiredHeatLoadMw} values2={data.series.plannedSupplyMw} color="#ee3d45" color2="#1479f5" min={1.5} max={3.3} unit="MW" ariaLabel={t('plannedVsRequired')} /></Panel><Panel title={t('indoorForecast')}><Legend items={[{ label: t('traditionalShort'), color: '#ee3d45' }, { label: t('optimisedShort'), color: '#1479f5' }, { label: t('comfortRange'), color: '#22b786', dashed: true }]} /><LineChart labels={data.series.labels} values={data.series.traditionalIndoorC} values2={data.series.optimisedIndoorC} color="#ee3d45" color2="#1479f5" min={16} max={26} unit="°C" ariaLabel={t('indoorForecast')} /></Panel><Panel title={interpolate(t('distributionAtTarget'), { time: data.forecastTargetTime })}><BarChart labels={bins.map((bin) => bin.label)} values={bins.map((bin) => bin.traditional)} values2={bins.map((bin) => bin.optimised)} unit={t('numberOfBuildings')} ariaLabel={interpolate(t('distributionAtTarget'), { time: data.forecastTargetTime })} /></Panel></div><div className="forecast-side"><Panel title={t('insights')}>{insights.map((insight, i) => <Insight key={insight.titleKey} number={String(i + 1)} title={t(insight.titleKey || insightTitles[i])} text={t(insight.textKey || insightTexts[i])} />)}</Panel><Panel title={t('keyForecastMetrics')}><div className="metric-list"><div><span>✓ {t('predictedCompliance')}</span><b>{pct(data.predictedComplianceRate)}</b></div><div><span>! {t('predictedOverheating')}</span><b className="text-red">{data.predictedOverheatingCount} / 12</b></div><div><span>❄ {t('predictedUnderheating')}</span><b>{data.predictedUnderheatingCount} / 12</b></div><div><span>⌂ {t('predictedComfort')}</span><b>{data.predictedComfortCount} / 12</b></div><div><span>↓ {t('oversupply')}</span><b>{kpis.avoidableOversupplyMw.toFixed(2)} MW</b></div></div></Panel><div className="callout-note"><b>{t('forecastBoundary')}</b><p>{t('forecastBoundaryText')}</p></div></div></div><Panel title={interpolate(t('buildingForecast'), { hours: horizon })}><div className="table-scroll"><table><thead><tr>{[t('building'), t('area'), t('year'), t('insulation'), t('zone'), t('currentTemp'), t('predictedTraditional'), t('predictedAi'), t('risk'), t('confidence')].map((header) => <th key={header}>{header}</th>)}</tr></thead><tbody>{rows.map((row) => { const building = buildings.find((item) => item.id === row.buildingId)!; return <tr key={row.buildingId}><td><b>{building.id}</b></td><td>{building.areaM2}</td><td>{building.year}</td><td>{t(building.insulation.toLowerCase())}</td><td>{t(building.zone)}</td><td>{building.indoorTemperatureC.toFixed(1)}°C</td><td>{row.predictedTraditionalC.toFixed(1)}°C</td><td>{row.predictedAiC.toFixed(1)}°C</td><td><span className={cx('risk-tag', row.risk === 'overheating' ? 'danger' : row.risk === 'underheating' ? 'cold' : 'safe')}>{t(row.risk === 'overheating' ? 'riskOverheating' : row.risk === 'underheating' ? 'riskUnderheating' : row.risk)}</span></td><td>{t(row.confidence.toLowerCase())}</td></tr>; })}</tbody></table></div></Panel></div>;
}

function Results({ t }: { t: T }) {
  const comparison = selectResultsComparison(); const traditional = comparison.kpis.traditional; const optimised = comparison.kpis.optimised; const percentiles = selectTemperaturePercentiles();
  const kpiLabels = [t('heatConsumption'), t('pumpElectricity'), t('compliance'), t('comfort'), t('overheating'), t('underheating')];
  return <div className="page"><div className="page-intro"><div><p className="kicker">{t('staticPrototype')}</p><h1>{t('resultsTitle')}</h1><p>{t('resultsDescription')}</p></div><span className="live-badge">{t('fixture')}</span></div><div className="kpi-grid results-kpis">{[[t('heatConsumption'), `${optimised[0].toFixed(1)} MWh`], [t('pumpElectricity'), `${optimised[1]} kWh`], [t('compliance'), `${optimised[2]}%`], [t('comfort'), `${optimised[3]}%`], [t('overheating'), `${optimised[4]}%`], [t('underheating'), `${optimised[5]}%`], [t('oversupply'), `${comparison.avoidableHeatLossMwh.toFixed(1)} MWh`], [t('hydraulic'), comparison.hydraulicBalance.optimised.toFixed(2)]].map(([label, value], i) => <KpiCard key={label} icon={['♨', '⚡', '✓', '⌂', '!', '❄', '↓', '⇄'][i]} label={label} value={value} note={i === 0 ? '−8.7%' : i === 1 ? '−17.1%' : i === 2 ? '+8.3 pp' : i === 3 ? '+33.4 pp' : i === 4 ? '−16.7 pp' : i === 5 ? '−8.3 pp' : i === 6 ? '~8.7%' : '+0.24'} tone={i === 4 || i === 5 ? 'green' : i === 6 ? 'blue' : 'green'} />)}</div><div className="content-grid results-grid"><Panel title={t('resultsTitle')} className="wide-panel"><Legend items={[{ label: t('traditionalShort'), color: '#9aa9ba' }, { label: t('optimisedShort'), color: '#1479f5' }]} /><BarChart labels={kpiLabels} values={traditional} values2={optimised} unit={t('valuePerMetric')} ariaLabel={t('resultsTitle')} /></Panel><Panel title={t('distribution')}><div className="percentile-row"><div><span>P10</span><b>{percentiles.p10.toFixed(1)}°C</b></div><div><span>P50 / Median</span><b>{percentiles.p50.toFixed(1)}°C</b></div><div><span>P90</span><b>{percentiles.p90.toFixed(1)}°C</b></div><div><span>Spread</span><b>{percentiles.spread.toFixed(1)}°C</b></div></div><LineChart labels={['P10', 'P50', 'P90']} values={[percentiles.p10, percentiles.p50, percentiles.p90]} values2={comparison.aiPercentileC} color="#9aa9ba" color2="#1479f5" min={16} max={26} unit="°C" ariaLabel={t('distribution')} /></Panel></div><div className="content-grid results-lower"><Panel title={t('flowDistribution')}><Legend items={[{ label: t('traditionalShort'), color: '#9aa9ba' }, { label: t('optimisedShort'), color: '#1479f5' }]} /><BarChart labels={zoneKeys.map((zone) => t(zone))} values={comparison.zoneFlow.traditional} values2={comparison.zoneFlow.optimised} unit={t('flowRate')} ariaLabel={t('flowDistribution')} /></Panel><Panel title={t('simulatedWindows')}><BarChart labels={zoneKeys.map((zone) => t(zone))} values={comparison.windowEvents.traditional} values2={comparison.windowEvents.optimised} unit={t('eventsCount')} ariaLabel={t('simulatedWindows')} /><p className="panel-note">{t('simulationOnly')}</p></Panel><Panel title={t('modelComparison')} subtitle={t('heldOut')} className="model-panel"><div className="table-scroll"><table><thead><tr><th>{t('model')}</th><th>{t('mae')}</th><th>{t('rmse')}</th><th>{t('r2')}</th></tr></thead><tbody>{modelMetrics.map((metric) => <tr key={metric.name}><td>{metric.name}</td><td><b>{metric.mae.toFixed(2)}</b></td><td>{metric.rmse.toFixed(2)}</td><td>{metric.r2?.toFixed(2)}</td></tr>)}</tbody></table></div><p className="panel-note">ⓘ {t('modelNote')}</p></Panel></div><div className="quote-card"><span>“</span><p>{t('explanation')}</p></div></div>;
}

function Settings({ t }: { t: T }) { return <div className="page"><div className="page-intro"><div><p className="kicker">{t('configuration')}</p><h1>{t('settingsTitle')}</h1><p>{t('settingsDescription')}</p></div><span className="live-badge">{t('fixtureProfile')}</span></div><div className="settings-grid"><Panel title={t('targets')}><div className="settings-fields">{[[t('comfortTarget'), '20–22 °C'], [t('complianceThreshold'), '18 °C'], [t('overheatThreshold'), '>23 °C'], [t('horizon'), '6 / 24 / 48'], [t('controlStep'), '30 minutes']].map(([label, value]) => <label key={label}>{label}<input value={value} readOnly /></label>)}</div></Panel><Panel title={t('limits')} subtitle={t('limitsNote')}><div className="limits-list"><div><span>{t('supply')}</span><b>{equipmentLimits.supplyTemperatureC.min}–{equipmentLimits.supplyTemperatureC.max}°C</b><em>{t('maxStep')} ±{equipmentLimits.supplyTemperatureC.step}°C</em></div><div><span>{t('pumpFrequency')}</span><b>{equipmentLimits.pumpFrequencyHz.min}–{equipmentLimits.pumpFrequencyHz.max} Hz</b><em>{t('maxStep')} ±{equipmentLimits.pumpFrequencyHz.step} Hz</em></div>{zoneKeys.map((zone) => <div key={zone}><span>{t(zone)} {t('valve')}</span><b>{equipmentLimits.valveOpeningPct.min}–{equipmentLimits.valveOpeningPct.max}%</b><em>{t('maxStep')} ±{equipmentLimits.valveOpeningPct.step} pp</em></div>)}</div></Panel><Panel title={t('nominalDelays')}><div className="delay-cards">{zoneKeys.map((zone) => <div key={zone} className={`delay-${zone}`}><b>{t(zone)}</b><strong>{fixtureNetwork.zones[zone].transportDelayMin} min</strong><span>{t('displayOnly')}</span></div>)}</div></Panel><Panel title={t('weights')}><div className="weights"><span>{t('objectiveCompliance')} <b>{t('high')}</b></span><span>{t('objectiveComfort')} <b>{t('high')}</b></span><span>{t('objectiveOversupply')} <b>{t('medium')}</b></span><span>{t('objectivePump')} <b>{t('medium')}</b></span><span>{t('objectiveMovement')} <b>{t('low')}</b></span></div></Panel></div></div>; }

function App() {
  const [path, setPath] = useState(() => window.location.pathname || '/overview'); const [language, setLanguage] = useState<Language>(() => (localStorage.getItem('ai-heating-language') as Language) || 'en'); const [mode, setMode] = useState<ControlMode>('optimised'); const [networkState, setNetworkState] = useState(getFixtureOptimisedState()); const [buildingStates, setBuildingStates] = useState(buildings.map((building) => ({ indoorTemperatureC: building.indoorTemperatureC }))); const [selectedBuildingId, setSelectedBuildingId] = useState('B03'); const [frameIndex, setFrameIndex] = useState(0); const [simulationTime, setSimulationTime] = useState(scenario.resetTime); const [isPlaying, setIsPlaying] = useState(false); const [advisoryApplied, setAdvisoryApplied] = useState(false); const [forecastHorizon, setForecastHorizon] = useState<ForecastHorizon>(6); const [events, setEvents] = useState<SemanticActionRecord[]>([{ type: 'simulation_started', at: scenario.resetTime, detailKey: 'detailScenario' }]);
  const t = useT(language); const buildingItems = useMemo(() => buildingProfiles.map((profile, i) => ({ ...profile, ...buildingStates[i] })), [buildingStates]); const selectedBuilding = buildingItems.find((building) => building.id === selectedBuildingId) ?? buildingItems[2];
  const [latestRecommendation, setLatestRecommendation] = useState(recommendationFixture);
  useEffect(() => { localStorage.setItem('ai-heating-language', language); document.documentElement.lang = language === 'zh' ? 'zh-CN' : 'en'; }, [language]); useEffect(() => { const onPop = () => setPath(window.location.pathname); window.addEventListener('popstate', onPop); return () => window.removeEventListener('popstate', onPop); }, []);
  const addEvent = (type: string, detailKey: string, detailValue?: string) => setEvents((current) => [{ type, at: simulationTime, detailKey, detailValue }, ...current]);
  const changeMode = (next: ControlMode) => { setMode(next); setAdvisoryApplied(false); addEvent('control_mode_changed', 'detailMode', t(next)); if (next === 'optimised') { setNetworkState((state) => applyPrototypeRecommendation(state)); addEvent('mpc_recommendation_generated', 'detailFixturePlanReady'); addEvent('mpc_control_auto_applied', 'detailOptimisedActive'); } };
  const apply = () => { if (mode !== 'advisory') return; setNetworkState((state) => applyPrototypeRecommendation(state)); setAdvisoryApplied(true); addEvent('recommendation_applied', 'detailOperatorConfirmed'); };
  const step = () => { const next = Math.min(frameIndex + 1, timeline.length - 1); setFrameIndex(next); setSimulationTime(timeline[next].time); if (mode === 'optimised') { setNetworkState((state) => advancePrototypeTimeline(state, timeline[next])); addEvent('mpc_control_auto_applied', 'detailTimelineFrame', timeline[next].time); } else addEvent('simulation_step', 'detailTimelineFrame', timeline[next].time); if (next === timeline.length - 1) setIsPlaying(false); };
  useEffect(() => { if (!isPlaying) return; const interval = window.setInterval(step, 1200); return () => window.clearInterval(interval); }, [isPlaying, frameIndex, mode, simulationTime]);
  const reset = () => { setIsPlaying(false); setFrameIndex(0); setSimulationTime(scenario.resetTime); setNetworkState(getFixtureOptimisedState()); setBuildingStates(buildings.map((building) => ({ indoorTemperatureC: building.indoorTemperatureC }))); setSelectedBuildingId('B03'); setForecastHorizon(6); setLatestRecommendation(recommendationFixture); setAdvisoryApplied(false); setEvents([{ type: 'simulation_reset', at: scenario.resetTime, detailKey: 'detailSimulationReset' }]); };
  const navigate = (next: string) => { window.history.pushState({}, '', next); setPath(next); }; const content = path.startsWith('/simulation') ? <Simulation t={t} recommendation={latestRecommendation} mode={mode} setMode={changeMode} networkState={networkState} setNetworkState={setNetworkState} buildingItems={buildingItems} selectedBuilding={selectedBuilding} setSelectedBuilding={(building) => setSelectedBuildingId(building?.id ?? 'B03')} onApply={apply} applied={advisoryApplied} frameIndex={frameIndex} simulationTime={simulationTime} isPlaying={isPlaying} setIsPlaying={setIsPlaying} onStep={step} events={events} onReset={reset} clearEvents={() => setEvents([])} /> : path.startsWith('/forecast') ? <Forecast t={t} horizon={forecastHorizon} forecastAsOf={simulationTime} setHorizon={setForecastHorizon} /> : path.startsWith('/results') ? <Results t={t} /> : path.startsWith('/settings') ? <Settings t={t} /> : <Overview t={t} recommendation={latestRecommendation} mode={mode} networkState={networkState} buildingItems={buildingItems} setSelectedBuilding={(building) => setSelectedBuildingId(building.id)} onApply={apply} applied={advisoryApplied} />;
  return <div className="app-shell"><aside className="sidebar"><div className="brand"><span className="brand-mark">◒</span><div><b>HeatPilot AI</b><small>{t('productSubtitle')}</small></div></div><nav>{[['/overview', '⌂', 'overview'], ['/simulation', '◇', 'simulation'], ['/forecast', '↗', 'forecast'], ['/results', '▥', 'results'], ['/settings', '⚙', 'settings']].map(([href, icon, key]) => <button key={href} className={path.startsWith(href) ? 'active' : ''} onClick={() => navigate(href)}><Icon symbol={icon} /><span>{t(key)}</span></button>)}</nav><div className="sidebar-footer"><span className="env-dot" />{t('environment')}<small>Pro · {t('districtHeating')}<br />{t('version')} 0.1.0</small></div></aside><main className="main"><header className="topbar"><div className="title-block"><h1>{t('productTitle')}</h1><p>{t('productSubtitle')}</p></div><div className="header-controls"><label><span>{t('scenario')}</span><select defaultValue="rapid"><option value="rapid">☀ {t('rapid')}</option></select></label><div className="header-mode"><span>{t('controlMode')}</span><ModeSelector mode={mode} setMode={changeMode} t={t} /></div><div className="header-time"><span>▣ {t('simulationTime')}</span><b>{scenario.date} {simulationTime}</b></div><button className="language-btn" aria-label={language === 'en' ? '中文' : 'English'} onClick={() => setLanguage(language === 'en' ? 'zh' : 'en')}>{language === 'en' ? '中文' : 'EN'}</button><span className="env-badge">⚗ {t('environment')}</span></div></header>{content}</main><button className="sr-only" onClick={reset}>{t('reset')}</button></div>;
}

export default App;
