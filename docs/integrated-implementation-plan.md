# Integrated HeatPilot implementation plan

Approved direction: 13 September 2026. Build and push a coherent Yinchuan-oriented engineering demonstrator, not claim a commissioned field twin.

1. Preserve existing research, reference imagery and public BIM assets. Make a new persistent dark operations workspace the default route. Keep engineering/reference routes for compatibility.
2. Add an authoritative registry linking demonstrator buildings, branch topology, simulation variables and scene selections. Provide real-site provenance and optional geometry/telemetry interfaces without inventing as-built mappings.
3. Extend the nonlinear engine with bounded numerical optimisation, re-run verification, time-resolved building forecasts, replay snapshots and expiring approval tokens. Preserve existing invariant-tested physics.
4. Add structured diagnostic and optimisation agent runs using pinned DeepSeek V4 Flash, authoritative world context, bounded tools and evidence-linked scene actions. No LLM field actuator. Numerical tools continue to work without provider access.
5. Implement authenticated read-only measurement ingestion, strict asset/metric/unit/time validation and freshness reporting. Keep measurements separate from uncalibrated simulation. External device integration requires operator mapping and commissioning.
6. Unify 3D selection, alarm navigation, contextual BIM inspection, trends, replay, forecast, agent evidence, simulator approval and exports in one workspace. Clearly distinguish public reference models, simulation and received observations.
7. Verify build, regression tests, optimisation invariants, mocked provider tool execution, telemetry rejection paths and desktop/mobile browser workflows. Run paid provider inference only as a bounded integration check if safely configured. Scan staged content for credentials, commit scoped changes and push the current feature branch without force.

Limits: no surveyed site geometry, licensed photogrammetry or operator telemetry credentials have been supplied. Shanghai mixed HVAC and Shenzhen district cooling are documented future system profiles, not enabled heating-model aliases. A field-ready system additionally requires site calibration, authenticated multi-user operations, durable infrastructure and operational commissioning.
